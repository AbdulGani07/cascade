name: Validate release tags

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

jobs:
  reject-unmanaged-tags:
    runs-on: ubuntu-latest
    steps:
      - name: Explain the supported release path
        run: |
          echo "::error::Release tags must be created by the protected Publish npm packages workflow."
          exit 1
