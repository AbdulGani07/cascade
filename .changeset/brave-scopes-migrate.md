---
"@cascade-code/cli": patch
---

Migrate every first-party npm package from the unavailable `@cascade` scope to the owner-controlled
`@cascade-code` scope. Update imports to `@cascade-code/*`; the `cascade` executable is unchanged.
