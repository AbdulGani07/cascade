# Changesets

Run `pnpm changeset` for every user-visible change. Select all affected public packages; the fixed
group configuration keeps their released versions aligned. State the user impact and migration steps,
not implementation trivia. Use `major` for incompatible schema, plugin API, CLI, export, or supported
Node.js changes.
